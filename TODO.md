# 意图系统 — 实验设计

> 本文件记录 Phase A 已完成实验和 Phase B 待设计实验。Phase A 原型验证了"意图→行动过滤"技术可行性，Phase B 实验验证"参谋长模式"设计可行性。

---

## Phase A — 原型验证（已完成）

### 实验 1：意图→行动过滤（HTML 原型）

**文件**：`examples/intent-prototype/index.html`

**目标**：验证"选定意图后，禁止选项直接消失"的交互机制在技术层面成立。

**流程**：玩家点意图按钮 → 行动面板按意图渲染对应选项 → 禁止选项消失（不灰掉） → 切换意图时消耗 +1 → 叙事文本切换变体。

**验收**：三个战役示例（帝丘店→积极防御、开封→保护平民、豫东第一阶段→收集情报）全部通过。

**结论**：✅ 行动面板随意图过滤的基本机制成立。

### 实验 2：情报→判断→意图 映射验证（LLM 参谋长）

**文件**：`examples/intent-llm-test/verify.py`

**目标**：验证"情报 → 参谋长判断 → 意图"这个映射链路是否自然成立——不需要硬编码规则表，仅凭情报文本就能输出合理判断。

**方法**：调用 LLM（扮演参谋长）读取三个基准战役的情报文本，输出判断和建议意图。

**结果**：3/3 匹配。帝丘店→积极防御，开封→保护平民，豫东第一阶段→收集情报。

**扩展测试**：6 个边缘场景（矛盾情报、过时情报、误导情报等），7/9 匹配。

**发现**：两个不匹配场景的共同特征是"情报质量低 → 参谋长退回收集情报"。这暴露了预设意图的盲区——预设意图假设信息足够做决策，但现实中"信息本身是否足够"这个前提没有被机制考虑。

**结论**：✅ 情报→判断→意图映射链路自然成立。不需要硬编码规则表。

---

## Phase B — 实验方案（待设计）

### B3 设计评审实验：参谋长模式原型

> 目标：为 B3 评审中的"选项 A（参谋长模式）vs 选项 B（极简模式）"决策提供可操作性指标。

实验名称为**"参谋长模式可行性验证"**，是一个数据驱动的设计评审工具，通过在已有情报系统输出上验证参谋长角色的交互可行性，B3 设计评审者可以比较两个模式的差异，做出更审慎的决策。

---

#### 实验 2.1：参谋长交互原型

**文件**：`examples/chief-of-staff-prototype/`

**目标**：构建一个交互原型，让玩家亲身体验"参谋长模式"的完整流程——不是看文档描述，而是实际操作一遍。

**流程**：
1. 开场：展示情报面板（情报卡片，带时效和可信度标记）
2. 参谋长介入：情报面板下方出现参谋长的分析框（不是弹窗、不是隐藏栏，是与情报并排的固定区域）
3. 参谋长输出：读取情报后给出局势判断 + 意图建议 + 理由
4. 玩家决策：玩家看到情报和参谋长的建议后，做自己的决策（点"构筑工事"、"疏散"等行动按钮，不是点意图按钮）
5. 系统反馈：玩家做决策后，系统显示"你的决策表明你的意图是 XXX"，意图标签作为分析结论出现
6. 切换成本：第二次以上决策时，显示时间推进一步、旧情报增加时效磨损

**角色区分**（UI 视觉区分是关键）：
| 元素 | 视觉风格 | 含义 |
|------|---------|------|
| 情报卡片 | 纯文本，灰底 | 原始情报，不含加工 |
| 参谋长分析框 | 虚线边框，不同底色 | 参谋长的分析，不是原始情报 |
| 行动按钮 | 实心按钮 | 玩家的决策入口 |
| 意图标签 | 纯标签，不可交互 | 系统反馈，不是操作入口 |

**关键约束**：
- 参谋长分析框不是"提示系统"——它的建议可能是错的（当情报误导时）
- 玩家不直接选意图，而是通过决策行为体现立场
- 意图标签是系统对玩家决策的反馈，不是前置选择

---

#### 实验 2.2：参谋长错误建议测试

**文件**：`examples/chief-of-staff-prototype/scenarios.json`

**目标**：验证当情报误导时，参谋长是否会给出错误建议，以及这种"错误"是否合理（是不是真的有迷惑性）。

**场景设计**：

| 场景 | 情报 | 参谋长建议 | 真实态势 | 玩家应做的判断 |
|------|------|-----------|---------|--------------|
| 基准场景 | 邱清泉 3 天后到 | 建议积极防御 | 真实 | 加快防御准备 |
| 误导场景 1 | 敌军番号不明，但侦察兵报告"看到大量坦克"（实际是假目标） | 建议收集情报 | 敌军已集结完毕 | 判断情报可靠性 |
| 误导场景 2 | 情报称"敌军向西撤退"（实际是迂回包抄） | 建议追击 | 敌在设伏 | 判断撤退真伪 |
| 矛盾场景 | 两份情报互相矛盾"敌军在增援" vs "敌军在撤退" | 建议收集情报 | 敌军分兵两路 | 判断哪个更可信 |
| 过时场景 | 情报是 3 天前的"敌在休整"（实际已出发） | 可能建议先侦察 | 敌军已离开 | 追求时效性 |

**验收标准**：
- 参谋长至少在 2 个场景中给出"合理但不正确"的建议
- 玩家能感受到"参谋长的分析是有价值的参考，但不是答案"
- 玩家不因为参谋长建议而丧失自主判断力

---

#### 实验 2.3：参谋长模式 vs 极简模式对比记录

**文件**：`examples/chief-of-staff-prototype/comparison.md`

**目标**：在同场景下记录两个模式的差异，供 B3 评审者决策。

| 评估维度 | 选项 A（参谋长模式） | 选项 B（极简模式） |
|---------|-------------------|------------------|
| 实现成本 | 高（需要情报系统就绪，参谋长分析逻辑，UI 分层） | 低（在现有原型上扩展，只需 JSON 配置） |
| 交互复杂度 | 中等（玩家看情报 → 看参谋长建议 → 决策） | 低（玩家直接点意图 → 行动面板变化） |
| 沉浸感 | 高（指挥官在指挥所的感觉） | 低（类似游戏菜单选择） |
| 对情报系统的依赖 | 强（没有情报系统，参谋长无输入可分析） | 弱（可以跑在兵棋引擎之上） |
| 迭代风险 | 低风险但高投入（方向正确，但做一半可能被卡住） | 低风险低投入（先跑通再优化） |
| 玩家学习成本 | 中等（需要理解"不直接选意图"的交互范式） | 低（意图按钮 + 面板变化，符合预期） |
| 独特价值 | 意图是判断的副产品，不是操作入口——这是核心设计目标 | 无，只是原型的技术验证 |

**对比结论**（待实验完成后填写）：
- ...

---

#### 验收

1. 玩家看到情报面板（不是意图按钮），参谋长在情报下方给出分析建议
2. 玩家不看参谋长的建议也能做决策（情报本身是充分的）
3. 玩家看过参谋长的建议后，决策更有依据感（参谋长不是多余信息）
4. 玩家通过点击行动按钮表达决策，系统从决策中推断意图并反馈
5. 误导场景中，参谋长给出"合理但不正确"的建议，玩家能识别
6. 整个交互中，玩家始终觉得自己在做决策，不是在选菜单

---

### 后续

- 如果 B3 选择选项 A：根据原型反馈，调整参谋长分析逻辑和 UI 分层，进入 Phase C 实现
- 如果 B3 选择选项 B：简化为意图按钮交互，先集成到兵棋引擎，后续迭代
- 无论 B3 选哪个，Phase D 的集成联调总是在情报系统就绪后才进行
