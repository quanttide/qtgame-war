# 领域模型

这四个模块构成了一个战棋游戏的领域模型基础，但作为“核心理论模型”，它目前更像一堆精心制作的零件，而缺失了将它们组装成能运转机器的总装图纸和引擎。

—

现有模块定位与质量

模块 定位 评分 关键问题
Battlefield 六边形几何 + 地形定义 ★★★★☆ 地图数据硬编码，无大碍但不可扩展
Unit 单位数据容器 ★★☆☆☆ 可变对象满天飞，类型定义松散（字符串阵营/特技）
Campaign 战役状态与修正 ★★☆☆☆ 被动状态篮，拼音命名，配置与状态混杂
Game 规则工具箱 ★★☆☆☆ 大杂烩：地形查询、移动、战斗、援军、胜判，却无回合

—

核心缺失：没有“回合游戏机”

这套模型定义了棋子（Unit）、棋盘（Battlefield）、天气/补给修正（Campaign）和规则书（Game 里的方法），但它缺了最关键的一样东西：

一局正在进行中的游戏本体——一个持有当前回合数、当前行动方、单位行动序列、能驱动“选择单位→移动→攻击→结束回合→AI回合→事件触发”的状态机。

目前的代码只能由外部脚本这样调用：

```dart
final range = game.getMoveRange(unit, allUnits);
// 玩家选了移动目标
unit.col = newCol; unit.row = newRow;
final targets = game.getAttackTargets(unit, allUnits);
// 玩家选了目标
final result = game.resolveCombat(attacker, defender, campaign);
// 手动扣血、手动标记行动结束...
```

没有任何结构保证这些操作按顺序发生、每个单位每回合只能行动一次、或回合结束时触发事件。 这就是你感觉“怪”的根源——模型只有原子能力，没有分子流程。

—

理想的核心模型架构

1. 不可变状态层（当前极缺）

· GameState：包含单位列表、当前回合、当前阶段（玩家移动/玩家攻击/AI移动/AI攻击）、战役状态、事件队列。
· UnitState 和 CampaignState 全部不可变。任何变动都产生新状态，使状态变迁可追踪、可回放、可测试。

2. 规则服务层（当前有，但被塞在 Game 里）

· MovementService：给定状态和单位，返回合法移动选项。
· CombatService：给定攻击者、防御者、地形、修正，返回结果（不修改状态）。
· VisibilityService：根据单位和地形更新揭示标记。

3. 战役和数据配置（当前耦合在代码中）

· ScenarioConfig：地图数据、初始单位模板、援军表、胜利条件。
· 所有剧本都从 JSON/YAML 加载，不硬编码。

4. 游戏控制器（当前完全缺失）

· GameController：接收玩家输入，调用服务，生成新状态，推进阶段和回合，处理 AI。
· 这是真正的“主线”，它是整个游戏的唯一驱动源。

—

这样一个核心模型能带来什么？

· 清晰的分层：渲染层只需观察 GameState 并绘制，用户操作通过 GameController 管道提交。
· 可测试性：服务是纯函数，状态是不可变数据，可以轻松模拟任何战局并断言结果。
· 扩展性：新剧本只需添加配置，新特技只需在 UnitAbility 和相应服务中增加逻辑，无需改动控制器骨架。
· 网络对战基础：不可变状态序列天然适合序列化和重放。

—

一句话总结

当前四个模块是战棋游戏的静态物理模型，但它们没有构成一个能自行运转的 回合制游戏核心。要成为真正的“核心理论模型”，必须引入 不可变状态 和 回合调度控制器，把散落的方法变成严格的状态转换管线。否则再多的地形、再细致的战斗公式，也只是没有发条的玩具兵。
